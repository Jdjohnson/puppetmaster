import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Puppet Master | Mostly Serious",
  description: "Can you guess the Mostly Serious team member behind the puppet? Play the guessing game or test your memory!",
  icons: {
    icon: "/icon.svg",
  },
  openGraph: {
    title: "Puppet Master | Mostly Serious",
    description: "Can you guess the Mostly Serious team member behind the puppet?",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
